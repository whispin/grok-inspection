# v0.1.10

## 更新说明

- 兼容 CPA 开启 TLS 时的禁用 / 启用 / 删除
- 本机禁用失败出现 EOF 等协议错误时，会自动改用 HTTPS 重试

## English

- Compatible with CPA TLS for disable / enable / delete
- Auto-retries with HTTPS when local disable fails with protocol errors such as EOF
