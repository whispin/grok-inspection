## 更新内容

- 优化手机端批量操作区，将横向滚动按钮改为两列网格布局，减少窄屏下的操作负担。
- 修正批量操作提示文字在手机端的布局，使其独占整行并支持长文本自动换行。
- 更新手机端布局回归测试，覆盖两列操作区和整行提示区域。

## 验证

- `go test ./... -count=1`
- 已在 CPA 桌面端和 `390x844` 手机视口验证页面布局。

**完整变更记录**：https://github.com/ywddd/grok-inspection/compare/v0.1.5...v0.1.6
